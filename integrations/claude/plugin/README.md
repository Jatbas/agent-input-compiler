AIC Claude Code Plugin — source for the Claude Code plugin that provides the AIC MCP server and lifecycle hooks. See [Installation — Claude Code](../../../documentation/installation.md#claude-code) for usage.

Version in `.claude-plugin/plugin.json` must be synced from root `package.json` at release.
