# aic-update-changelog examples

| File                         | Status | Notes                                                                                                                                                                                                    |
| ---------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `changelog-entry-example.md` | TODO   | Snippet showing before/after `CHANGELOG.md` under both `update` mode (append to `[Unreleased]`) and `release` mode (promote to versioned heading). Must pass `shared/scripts/changelog-format-check.sh`. |

See `.claude/skills/shared/examples/README.md`.
