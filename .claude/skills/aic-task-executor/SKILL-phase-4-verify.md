# Phase 4: Verify

### 4. Verify

After completing all steps, run a single verification pass using tool output as objective evidence.

**4a — Run toolchain (parallel).**

Run the full toolchain as one parallel batch. All five jobs are pure readers on source files (no shared writes), so running them concurrently is safe and saves wall-clock time — on a cold typecheck + test run this is typically 1.8–2.5× faster than the old `&&` chain. Capture per-job output so failures remain identifiable and the log is not interleaved:

Copy-paste the runner verbatim — there are no manual substitutions. `MAIN_ROOT` auto-resolves to the main workspace root (the parent of the common `.git` directory), which is where `pnpm knip` must run to avoid the worktree path-resolution false positives called out in §1.

```
bash -c '
  set +e
  MAIN_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")"
  D=$(mktemp -d)
  run() { local n=$1; shift; ("$@" >"$D/$n.log" 2>&1; echo $? >"$D/$n.rc") & }
  run lint       bash -lc "source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm lint"
  run typecheck  bash -lc "source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm typecheck"
  run test       bash -lc "source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm test"
  run knip       bash -lc "cd \"$MAIN_ROOT\" && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm knip"
  run clones     bash -lc "source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm lint:clones"
  wait
  FAIL=""
  for n in lint typecheck test knip clones; do
    rc=$(cat "$D/$n.rc")
    if [ "$rc" -ne 0 ]; then
      echo "--- $n FAILED (exit $rc) ---"; cat "$D/$n.log"; echo "--- end $n ---"
      FAIL="$FAIL $n"
    else
      echo "--- $n ok ---"
    fi
  done
  [ -z "$FAIL" ] || { echo "FAILED:$FAIL"; exit 1; }
'
```

**Confirm:** Zero errors + warnings. Test count not dropped. Each Tests table name in output. No new knip findings. Zero code clones.

Runs ONCE. On failure, re-read every `--- <job> FAILED ---` section (parallel execution surfaces ALL failures in one pass, not just the first — use this as a diagnostic advantage), fix all of them, then re-run the full parallel batch.

**If you cannot run shell heredocs in your agent environment** (e.g. a restricted tool surface), fall back to the sequential form with the same Node and knip caveats. `MAIN_ROOT` auto-resolves; copy verbatim:

```
MAIN_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" \
  && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm lint \
  && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm typecheck \
  && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm test \
  && (cd "$MAIN_ROOT" && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm knip) \
  && source .claude/skills/shared/scripts/ensure-supported-node.sh && pnpm lint:clones
```

Parallel is the default; sequential is only a last-resort fallback and must be noted in §5a first-pass tracking ("§4a ran sequentially — tool surface restriction").

**Known issue — knip false positives in `.git-worktrees/` paths.** Always run `pnpm knip` from the **main workspace root**, not the worktree (see §1 worktree caveat). §4a still runs before §5 finalize and §6 merge; knip simply runs against the main workspace's view of the worktree branch (it reads committed files from the branch). Do not investigate worktree-specific knip failures — they are path-resolution artifacts.

**4b — Re-read all files + mechanical checks in one parallel batch.**

Fire all in one parallel batch — Read every created/modified file + batch all Grep checks below. Fix violations immediately, then re-run only the failed checks.

| Dim                                                               | Check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Evidence                                                                                       |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| 1. Signature match                                                | Re-read interface + implementation side by side. Extract every `.methodName(` call and Grep interface file — 0 matches = MISMATCH. For composition roots: verify every `new ClassName(...)`, exported fn signature, library import/call against Wiring Spec. This catches training-data contamination where you call a method that exists on the underlying library but not on the project's interface wrapper. pnpm typecheck is the primary safety net, but this mechanical extraction catches issues in untyped contexts (test casts, SQL column names, prose instructions)                                                                                     | Each method — MATCH/MISMATCH. Each extracted call with Grep count                              |
| 2. Readonly / mutability                                          | Grep production files for `\.push\(`, `\.splice\(`, `\.sort\(`, `\.reverse\(`. Grep for array types missing `readonly` (`: [A-Z]\w+\[\]` without preceding `readonly`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Grep output ("0 matches" = pass)                                                               |
| 3. Branded types                                                  | Grep for factory function usage (`toTokenCount`, `toRelativePath`, etc.) in impl + test files. Grep for raw literals in type positions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Factory usage evidence or raw values found                                                     |
| 4. Comment style                                                  | Grep for `/\*\*` and `/\*[^/]` in new files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Grep output ("0 matches" = pass)                                                               |
| 5. DI & immutability                                              | Re-read constructor — list each param as interface or concrete. For composition roots: verify `new` only in composition root file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Each constructor param with type                                                               |
| 6. Tests complete                                                 | Re-read test file — list every `it(`/`test(` name. Cross-check against Tests table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Tests table row → test name (or MISSING)                                                       |
| 7. Config changes                                                 | Re-read `shared/package.json` and `eslint.config.mjs` if task required changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Each change — present or missing                                                               |
| 8. Lint + typecheck + tests + knip                                | Reference §4a output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | "Passed in §4a" or paste output                                                                |
| 9. ESLint gaps                                                    | Grep for untyped `export const \w+ = {`. Grep for `else if` chains (3+ branches)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Grep output ("0 matches" = pass)                                                               |
| 10. Layer boundaries                                              | Grep for banned import patterns in new files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Grep output ("0 matches" = pass)                                                               |
| 11. No `let` in production                                        | Grep for `^\s*let ` in production files. Only boolean control flags acceptable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Grep output ("0 matches" = pass)                                                               |
| 12. Zero code clones                                              | Reference `pnpm lint:clones` from §4a. Extract shared utilities if clones found                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | "0 clones" or clones + resolution                                                              |
| 13. SQL determinism                                               | Grep for `date\('now'\)` and `datetime\('now'\)` in storage files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Grep output ("0 matches" = pass)                                                               |
| 14. Orphan test files                                             | Glob for `*.test.ts` NOT under `__tests__/` in `shared/src/`, `mcp/src/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Each orphan + resolution, or "0 orphans"                                                       |
| 15. Conditional deps (composition roots only)                     | Grep for `new ` and `.create(` — conditional deps must be injected, not eagerly instantiated. N/A if no composition root modified                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Each call — "always needed" / "conditional — injected via [param]"                             |
| 16. Transformer benchmark (if transformer added)                  | Run the relevant integration benchmark(s) declared by the task. Token-changing transformers include token-reduction baseline verification. Test auto-ratchets declared benchmark artifacts only. Commit ratcheted artifacts only when allowed by the task file or §5c whitelist. N/A if no transformer                                                                                                                                                                                                                                                                                                                                                             | "Benchmark [name]: PASS. Baseline/artifact N→M (delta -X%) or unchanged"                       |
| 17. Transformer safety (if transformer added)                     | Grep test file for `safety_*` tests. Non-format-specific: one per sensitive type. Format-specific: one per extension                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Each `safety_*` test, or "N/A"                                                                 |
| 18. DB normalization (if migration added)                         | Check 1NF (no multi-value TEXT), 2NF (no partial key deps), 3NF (no transitive deps). Flag missing lookup tables and redundant columns                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | "[table] — 1NF/2NF/3NF: PASS/VIOLATION" or "N/A"                                               |
| 19. Stale markers (informational)                                 | Grep production files for `TODO`, `FIXME`, `HACK`, stale phase heading refs (`Phase (?:[A-Z]{1,2}\|[0-9]+(?:\.[0-9]+)?)\b` — documentation-writer Dimension 9). Fix markers in new code; report pre-existing as follow-up                                                                                                                                                                                                                                                                                                                                                                                                                                          | Each marker — ACTIONABLE/INFORMATIONAL                                                         |
| 20. String references (informational for pre-existing)            | For renames: grep codebase for old name in dispatch keys, errors, logs, test names, configs (`vitest.config.ts`, `tsconfig.json`, `package.json`). Blocking if executor introduced the rename                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Each ref — STALE/CURRENT, or "N/A"                                                             |
| 21. Non-TS assets (if introduced)                                 | Verify: build copies asset to `dist/`, CI runs `pnpm build`, vitest aliases resolve to `src/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | "[asset] — build: Y/N, CI: Y/N, alias: Y/N" or "N/A"                                           |
| 22. Goal-traceability acceptance                                  | Open `## Goal`, `## Behavior Change`, `## Tests`, and `## Acceptance Criteria`. Verify every task-specific acceptance bullet maps to a concrete post-change proof artifact — a named test present in §4a test output, command output you actually printed, an exported symbol in a produced file, a schema/descriptor field, a JSON/payload field, a log line, a migration/schema row, an MCP tool response, or a file path that now exists. Generic invariants (`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm knip`, no `let`) do NOT satisfy goal traceability. Applies to every current task file; legacy N/A requires an explicit pre-2026 task-file note. | Each task-specific acceptance bullet → proof artifact — MAPPED / UNMAPPED. 0 unmapped required |
| 23. Boundary contract mirrors (if runtime contract shape changed) | If a task changes an exported type, structured payload field, DTO, runtime JSON shape, cache blob, telemetry event, diagnostic payload, or tool/API response, grep changed field names, at least two sibling fields from the same object shape, and boundary terms (`z.object`, `outputSchema`, `inputSchema`, `structuredContent`, `JSON.stringify`, `schema`, `validator`, `payload`, `registerTool`, `descriptor`, `metadata`, `cache`, `telemetry`). Every schema/validator/descriptor/formatter/parser/test/manual mirror found must be in the Files table or explicitly justified as compatible in the task. Out-of-list mirrors are a Blocked diagnostic.   | Each mirror — IN FILES TABLE / COMPATIBLE BY TASK EVIDENCE / BLOCKED (path + reason)           |

**4c — Confirm clean and track first-pass quality.**

**Gate Function:** For every claim: (1) run the tool command, (2) read the actual output, (3) verify it matches success criteria, (4) then claim the result. No memory-based claims.

After §4b, every dimension must be clean. Unchecked dimensions = skipped — go back and check. Architectural issues that cannot be fixed mechanically → **Blocked diagnostic**.

**First-pass tracking:** Record whether each dimension was clean on first check or needed a fix. Report in §5a (e.g. "22/22 first-pass clean" or "19/22, fixed 3: readonly array in X, block comment in Y, unmapped goal bullet in Z"). Exclude inapplicable conditional dimensions (16-18, 21, and 23 when no runtime contract shape changed) from denominator. Dimension 22 applies to every current task file; use N/A only for explicitly identified legacy tasks. Dims 19-20 are informational — always in denominator but don't block for pre-existing issues. Quick-check catches in §3 count as first-pass clean.

Once all dimensions are confirmed clean, proceed to §5.

---

**Emit the verification-complete checkpoint now.** Run this exactly:

```
echo "CHECKPOINT: aic-task-executor/verification-complete — complete"
bash .claude/skills/shared/scripts/checkpoint-log.sh \
  aic-task-executor verification-complete "<short-artifact-note>"
```

The artifact note is free-text (e.g. `"§4a lint typecheck test knip clones"`). Do not proceed to Phase 5 until this command exits 0.

Phase 4 complete. Read `SKILL-phase-5-finalize.md` and execute it immediately.
